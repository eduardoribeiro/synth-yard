{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "isolatedModules": true,
    "types": ["jest", "node"]
  },
  "include": ["server/**/*.js", "server/**/*.ts"],
  "exclude": ["server/data", "server/gcode"]
}
